import React from 'react'

const SectionTitle = ({ title }) => {
    return (
        <h1 className='section-title'>{title}<span className='text-site-gold'>.</span></h1>
    )
}

export default SectionTitle